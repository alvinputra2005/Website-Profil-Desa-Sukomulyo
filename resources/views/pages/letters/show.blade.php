<x-layouts.app :title="$letterService->name" :description="$letterService->description">
    <section class="letter-hero compact"><div class="container"><a class="letter-back" href="{{ route('letter-services.index') }}">← Semua layanan</a><span class="letter-kicker">{{ $letterService->code }}</span><h2>{{ $letterService->name }}</h2><p>{{ $letterService->description }}</p></div></section>
    <section class="letter-section"><div class="container letter-two-column"><div class="letter-card"><h3>Persyaratan yang perlu disiapkan</h3><ul class="letter-requirements">@foreach($letterService->requirements_json as $requirement)<li><i class="fas fa-check-circle"></i><span><strong>{{ $requirement['label'] }}</strong>@if($requirement['description'] ?? null)<small>{{ $requirement['description'] }}</small>@endif</span></li>@endforeach</ul></div>
    <aside class="letter-card letter-summary"><h3>Informasi layanan</h3><p><strong>Estimasi:</strong> {{ $letterService->processing_days }} hari kerja</p><p><strong>Biaya:</strong> {{ $letterService->fee_information }}</p><p><strong>Jam pelayanan:</strong> {{ $settings->officeHours() }}</p><p><strong>Pengambilan:</strong> {{ $settings->pickupAddress() }}</p>@if($letterService->pickup_instructions)<p>{{ $letterService->pickup_instructions }}</p>@endif<a class="letter-button" href="{{ route('letter-services.application.create', $letterService) }}">Ajukan Permohonan Surat</a><small>Surat diberikan secara fisik dan tidak tersedia untuk diunduh.</small></aside></div></section>
</x-layouts.app>
