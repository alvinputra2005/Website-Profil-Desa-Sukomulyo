@extends('layouts.admin')@section('title','Baca Pesan')@section('content')<div class="box box-primary"><div class="box-header with-border"><h3 class="box-title">Pesan dari {{ $message->name }}</h3><div class="box-tools pull-right"><a href="{{ route('admin.messages.index') }}" class="btn btn-box-tool"><i class="fa fa-times"></i></a></div></div><div class="box-body no-padding"><div class="mailbox-read-info"><h3>{{ $message->name }}</h3><h5>Dari: <a href="mailto:{{ $message->email }}">{{ $message->email }}</a>@if($message->phone) · {{ $message->phone }}@endif<span class="mailbox-read-time pull-right">{{ $message->created_at->format('d M Y H:i') }}</span></h5></div><div class="mailbox-read-message" style="white-space:pre-wrap">{{ $message->message }}</div></div><div class="box-footer"><a href="{{ route('admin.messages.index') }}" class="btn btn-default"><i class="fa fa-reply"></i> Kembali</a><form method="post" action="{{ route('admin.messages.destroy',$message) }}" data-confirm="Hapus pesan ini?" class="pull-right">@csrf @method('delete')<button class="btn bg-maroon"><i class="fa fa-trash-o"></i> Hapus</button></form></div></div>@endsection
