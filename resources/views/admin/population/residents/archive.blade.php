@extends('layouts.admin')
@section('title','Arsip Penduduk')
@section('content')
<div class="box box-warning"><div class="box-header with-border"><h3 class="box-title"><i class="fa fa-archive"></i> Arsip Penduduk</h3><div class="box-tools"><a href="{{ route('admin.population.residents.index') }}" class="btn btn-default btn-sm"><i class="fa fa-arrow-left"></i> Data Aktif</a></div></div><div class="box-body"><form method="get" class="form-inline"><input name="q" class="form-control" value="{{ request('q') }}" placeholder="Cari NIK / nama"> <button class="btn btn-primary">Cari</button></form><div class="table-responsive"><table class="table table-striped"><thead><tr><th>NIK</th><th>Nama</th><th>Status terakhir</th><th>Diarsipkan</th><th>Aksi</th></tr></thead><tbody>@forelse($residents as $resident)<tr><td>{{ $resident->nik }}</td><td>{{ $resident->name }}</td><td>{{ $resident->status }}</td><td>{{ $resident->deleted_at?->format('d-m-Y H:i') }}</td><td><form method="post" action="{{ route('admin.population.residents.restore',$resident) }}" class="inline-form">@csrf @method('patch')<button class="btn btn-success btn-xs"><i class="fa fa-undo"></i> Pulihkan</button></form> <form method="post" action="{{ route('admin.population.residents.force-delete',$resident) }}" class="inline-form" data-confirm="Hapus permanen data penduduk ini? Tindakan tidak dapat dibatalkan." data-confirm-tone="danger" data-confirm-title="Hapus Permanen" data-confirm-button="Ya, hapus permanen">@csrf @method('delete')<button class="btn btn-danger btn-xs"><i class="fa fa-trash"></i> Hapus</button></form></td></tr>@empty<tr><td colspan="5" class="empty-state">Belum ada arsip penduduk.</td></tr>@endforelse</tbody></table></div>{{ $residents->links() }}</div></div>
@endsection
