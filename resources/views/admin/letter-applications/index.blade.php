@extends('layouts.admin')
@section('title','Permohonan Surat')
@section('content')
<div class="box box-primary"><div class="box-header with-border"><h3 class="box-title">Daftar Permohonan</h3></div><div class="box-body">
<form class="row" method="get"><div class="col-md-3 form-group"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Nomor atau nama pemohon"></div><div class="col-md-2 form-group"><select class="form-control" name="status"><option value="">Semua status</option>@foreach(\App\Enums\LetterApplicationStatus::cases() as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select></div><div class="col-md-3 form-group"><select class="form-control" name="service"><option value="">Semua layanan</option>@foreach($services as $service)<option value="{{ $service->id }}" @selected((string)request('service')===(string)$service->id)>{{ $service->name }}</option>@endforeach</select></div><div class="col-md-2 form-group"><button class="btn btn-primary">Terapkan filter</button></div></form>
<div class="table-responsive"><table class="table table-striped table-hover"><thead><tr><th>Nomor</th><th>Pemohon</th><th>Layanan</th><th>Tanggal</th><th>Status</th><th>Petugas</th><th></th></tr></thead><tbody>@forelse($applications as $application)<tr><td><strong>{{ $application->application_number }}</strong></td><td>{{ $application->applicant_name }}</td><td>{{ $application->service->name }}</td><td>@if($application->submitted_at){{ $application->submitted_at->timezone('Asia/Jakarta')->format('d/m/Y H:i') }}@else<span class="text-muted">Belum dikirim</span><br><small>Dibuat {{ $application->created_at->timezone('Asia/Jakarta')->format('d/m/Y H:i') }}</small>@endif</td><td><span class="label {{ $application->status->badgeClass() }}">{{ $application->status->label() }}</span></td><td>{{ $application->assignee?->name ?? 'Belum ditugaskan' }}</td><td><a class="btn btn-xs btn-primary" href="{{ route('admin.letter-applications.show',$application) }}">Detail</a></td></tr>@empty<tr><td colspan="7" class="text-center">Belum ada permohonan.</td></tr>@endforelse</tbody></table></div>{{ $applications->links() }}</div></div>
@endsection
